---
phase: 01-ecosystem-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ecosystem/build-system/module.mk
  - ecosystem/build-system/common.mk
  - ecosystem/build-system/README.md
  - ecosystem/build-system/examples/foundation-libft.mk
autonomous: true
requirements:
  - TS-1
must_haves:
  truths:
    - "Um modulo consegue compilar com make sem duplicar logica de build"
    - "Um modulo consegue rodar testes via make test"
    - "Um modulo consegue instalar headers e lib via make install"
  artifacts:
    - path: "ecosystem/build-system/module.mk"
      provides: "Alvos padrao all/test/clean/install"
    - path: "ecosystem/build-system/common.mk"
      provides: "Defaults de compilacao e variaveis compartilhadas"
    - path: "ecosystem/build-system/examples/foundation-libft.mk"
      provides: "Exemplo canonico de integracao"
  key_links:
    - from: "foundation/*/Makefile"
      to: "ecosystem/build-system/module.mk"
      via: "include"
      pattern: "include .*module.mk"
    - from: "module.mk"
      to: "common.mk"
      via: "include"
      pattern: "include .*common.mk"
---

<objective>
Entregar a espinha dorsal de build reutilizavel para todo o ecossistema.

Purpose: Eliminar duplicacao de Makefiles e garantir targets uniformes.
Output: camada de build compartilhada com exemplo funcional de integracao.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ecosystem-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Definir camada base de variaveis e flags em common.mk</name>
  <files>ecosystem/build-system/common.mk</files>
  <action>Criar `common.mk` com defaults seguros para `CC`, `AR`, `CFLAGS_DEBUG`, `CFLAGS_RELEASE`, `BUILD_MODE`, `PREFIX` e diretorios derivados. Evitar paths absolutos e suportar override por ambiente. Incluir validacao de modo de build para impedir valores invalidos.</action>
  <verify>
    <automated>make -f ecosystem/build-system/common.mk -n all || true</automated>
  </verify>
  <done>Arquivo exporta defaults reutilizaveis e aceita override sem quebrar builds existentes.</done>
</task>

<task type="auto">
  <name>Task 2: Criar module.mk com targets padrao do ecossistema</name>
  <files>ecosystem/build-system/module.mk</files>
  <action>Implementar `all`, `test`, `clean`, `install` em `module.mk` com consumo de metadata do modulo (`MODULE_NAME`, `SRC_DIR`, `INCLUDE_DIR`, `TEST_DIR`). Incluir compilacao para biblioteca esttica e instalacao de headers/lib em `$(PREFIX)` com suporte a `DESTDIR`.</action>
  <verify>
    <automated>make -f ecosystem/build-system/module.mk -n all; make -f ecosystem/build-system/module.mk -n test; make -f ecosystem/build-system/module.mk -n install</automated>
  </verify>
  <done>Targets padrao estao definidos e permitem integracao uniforme dos modulos.</done>
</task>

<task type="auto">
  <name>Task 3: Documentar e exemplificar uso no modulo foundation</name>
  <files>ecosystem/build-system/README.md, ecosystem/build-system/examples/foundation-libft.mk</files>
  <action>Adicionar README com contrato minimo de integracao (variaveis obrigatorias/opcionais) e criar exemplo de Makefile para `foundation/libft` incluindo `module.mk` per TS-1.</action>
  <verify>
    <automated>rg "MODULE_NAME|include .*module.mk|make test|make install" ecosystem/build-system/README.md ecosystem/build-system/examples/foundation-libft.mk</automated>
  </verify>
  <done>Existe referencia clara para qualquer novo modulo adotar o build compartilhado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev shell -> make | Variaveis de ambiente entram no build e podem alterar comportamento |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | ecosystem/build-system/common.mk | mitigate | Validar `BUILD_MODE` contra whitelist e usar defaults seguros |
| T-01-02 | E | install target | mitigate | Restringir install a `PREFIX`/`DESTDIR` explicitos sem comandos externos dinamicos |
| T-01-03 | D | test target | accept | Falha de teste interrompe pipeline local; impacto esperado e desejado |
</threat_model>

<verification>
- `make -f ecosystem/build-system/module.mk -n all` executa sem erro de sintaxe
- `make -f ecosystem/build-system/module.mk -n test` mostra fluxo de teste padrao
- `make -f ecosystem/build-system/module.mk -n install` respeita PREFIX/DESTDIR
</verification>

<success_criteria>
- Build compartilhado cobre `all/test/clean/install`
- Integracao com modulo de exemplo documentada
- TS-1 parcialmente satisfeito (base de execucao pronta para adocao nos modulos)
</success_criteria>

<output>
After completion, create `.planning/phases/01-ecosystem-infrastructure/01-01-SUMMARY.md`
</output>
